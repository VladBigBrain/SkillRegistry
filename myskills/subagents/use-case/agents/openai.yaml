interface:
  display_name: "Use Case"
  short_description: "UC template + BA↔DDD mapping (command, aggregate, events)"
  default_prompt: "Use $use-case to draft a Use Case (UC) with main/alternate flows and a BA↔DDD mapping."

policy:
  allow_implicit_invocation: true
