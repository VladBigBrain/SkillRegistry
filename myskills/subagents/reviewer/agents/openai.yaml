interface:
  display_name: "Reviewer"
  short_description: "Review implementation against the artifact and plan"
  default_prompt: "Use $reviewer to assess whether the implementation matches the analytical artifact and approved plan, identify findings, and propose fixes."

policy:
  allow_implicit_invocation: false
